spring:
  profiles:
    active: prod
---
spring:
  profiles: prod
  application:
    name: licensingservice
  cloud:
     config:
       uri: http://host.docker.internal:10703
       label: master
       profile: prod
---
spring:
  profiles: dev
  application:
    name: licensingservice
  cloud:
     config:
       uri: http://localhost:10703
       label: master
       profile: dev
